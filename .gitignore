# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Worktrees
.worktrees

# Superpowers brainstorm files
.superpowers

# Claude Code per-machine permission allowlist — auto-modified by the CLI when
# the user accepts "always allow" on a tool prompt. Tracking it causes pull
# conflicts when sessions auto-append patterns. Each machine keeps its own copy.
.claude/settings.local.json
